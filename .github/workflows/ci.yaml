name: CI

on:
  push:
    branches: [ main, develop, 'test-*', 'ci-test-*' ]
  pull_request:
    branches: [ main ]

jobs:
  ci:
    runs-on: ubuntu-latest
    strategy:
      matrix:
        python-version: ["3.10", "3.11", "3.12", "3.13"]

    steps:
      - name: Checkout repo
        uses: actions/checkout@v4

      - name: Set up Python ${{ matrix.python-version }}
        uses: actions/setup-python@v5
        with:
          python-version: ${{ matrix.python-version }}

      - name: Cache pip
        uses: actions/cache@v4
        with:
          path: ~/.cache/pip
          key: ${{ runner.os }}-pip-${{ hashFiles('**/pyproject.toml', '**/requirements/*.txt') }}
          restore-keys: |
            ${{ runner.os }}-pip-

      - name: Install dependencies
        run: |
          pip install --root-user-action=ignore --upgrade pip
          pip install --root-user-action=ignore git+https://github.com/kvernet/procela.git@v1.0.2
          pip install --root-user-action=ignore -v -e .[dev]

      - name: Run pre-commit
        run: pre-commit run --all-files --show-diff-on-failure

      - name: Run mypy (with relaxed scipy settings)
        run: |
          mypy procela_analysis \
            --python-version ${{ matrix.python-version }} \
            --ignore-missing-imports \
            --check-untyped-defs \
            --no-implicit-optional

      - name: Run tests with coverage
        run: |
          pytest -p no:warnings tests/ \
            -v \
            --cov=procela_analysis \
            --cov-report=term-missing \
            --cov-report=xml \
            --junit-xml=junit.xml

      - name: Upload coverage to Codecov
        uses: codecov/codecov-action@v4
        with:
          file: ./coverage.xml
          flags: unittests
          name: codecov-umbrella

      - name: Upload test results
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: test-results-${{ matrix.python-version }}
          path: |
            junit.xml
            htmlcov/
